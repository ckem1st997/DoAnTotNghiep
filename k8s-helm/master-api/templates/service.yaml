apiVersion: v1
kind: Service
metadata:
  name: {{ include "master-api.fullname" . }}
  labels:
    {{- include "master-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "master-api.selectorLabels" . | nindent 4 }}
